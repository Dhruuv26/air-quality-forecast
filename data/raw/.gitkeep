# keeps this directory present in git even when empty (git doesn't track
# empty folders) — the ingestion scripts also create it automatically now,
# but this ensures it's there from the very first checkout
